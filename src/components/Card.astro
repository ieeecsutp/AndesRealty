---
const { title, price, imageUrl, link } = Astro.props;
---

<div class="max-w-64 text-gray-200 border bg-primary border-gray-200 rounded-lg shadow-sm dark:bg-primary dark:border-gray-700">
    <a href={link}>
        <img class="rounded-t-lg w-full h-48 object-cover" src={imageUrl} alt={title} />
    </a>
    <div class="p-5">
        <a href={link}>
            <h5 class="mb-2 text-2xl font-bold tracking-tight dark:text-gray-200">{title}</h5>
        </a>
        <p class="mb-3 font-normal text-white">S/. {price}</p>
        <a href={link} class="inline-flex items-center px-3 py-2 text-sm font-medium text-center text-white bg-accent rounded-lg hover:bg-accent-light focus:ring-4 focus:outline-none focus:ring-gray-500 dark:bg-primary dark:hover:bg-primary dark:focus:ring-gray-500">
            Ver más
            <svg class="rtl:rotate-180 w-3.5 h-3.5 ms-2" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 10">
                <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 5h12m0 0L9 1m4 4L9 9"/>
            </svg>
        </a>
    </div>
</div>
